/**
 * AiPlayer.java
 * Lucas Weisensee
 *
 * AiPlayer is an abstract class from which all AI players are derived
 *     -includes the worker process's id
 */
public abstract class AiPlayer extends Client {
    public int PROCESS_ID;
}
